extends layout
block content
  each post in posts
    article.post
      header.post-header
        h1.post-title
          a(href='/' + post.slug)!= post.title
        section.post-meta
          time.post-date(datetime=post.prettyDate)!= post.prettyDate
          a(href='/author/' + post.author)!= post.author
      section.post-excerpt
        p!= post.summary
          | ...
          a.read-more(href='/' + post.slug) »
